<?xml version="1.0" encoding="utf-8"?>
<!-- Debug-only: permit cleartext HTTP for local dev servers (emulator, LAN).
     Release builds fall back to src/main/res/xml/network_security_config.xml, which remains strict. -->
<network-security-config>
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
